name: Build and Deploy Container

on:
  push:
    branches:
      - main

defaults:
  run:
    working-directory: container

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Install dependencies
        run: npm install
      - name: Build React App
        run: npm run build
      - name: Cài đặt AWS CLI
        uses: unfor19/install-aws-cli-action@v1
      - name: test biến môi trường
        run: echo ${{ secrets.AWS_S3_BUCKET_NAME }}
      - name: Go to bucket 
        run: aws s3 sync dist s3://mfe-dashboard-tn/container/lastest
        env:
          AWS_ACCESS_KEY_ID: AKIAQ4ML26UW45DQSOOJ
          AWS_SECRET_ACCESS_KEY: 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
          AWS_DEFAULT_REGION: "ap-southeast-1"
