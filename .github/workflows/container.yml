name: Build and Deploy Container

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        run: cd container 
            npm install
            npm run build